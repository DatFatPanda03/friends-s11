# Friends S11 reader

A single-page reader for "The One After Rachel Got Off the Plane".
The whole script is embedded in `index.html` -- no build step, no dependencies.
Reading position is stored per-device in localStorage.
